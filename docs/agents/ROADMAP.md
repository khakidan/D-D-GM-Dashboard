# Roadmap

Referenced from the root [AGENTS.md](../../AGENTS.md). This file tracks **only currently-open work** — pending features/bugs and in-progress/scoped-but-not-yet-built plans. Read this file (not `CHANGELOG.md`) to know what's currently being worked on or planned next.

Per root AGENTS.md rule 12: when something here is completed, it gets **removed entirely** from this file (not archived here) and a write-up documenting what was actually built gets added to [CHANGELOG.md](CHANGELOG.md) instead. This file should stay small and fully current — if a section here says "Completed," that's a sign it should have already been moved out.

---

## Pending Features

Features and bugs that have been discussed and approved but not yet implemented. Each entry contains enough context to implement without further discussion.

### 🔴 Bugs to Fix

None currently open.

### 🟡 Features to Add

**Full Codebase Audit (bugs → componentization → UI uniformity).** Comprehensive pass for logic errors/bugs, oversized files needing decomposition, and UI/UX uniformity closer to established D&D apps (D&D Beyond, Roll20 conventions). Prioritized in this order: (1) bug-hunting/logic errors, (2) file size & componentization, (3) UI uniformity/D&D-app UX conventions. **Phase 1 (bug-hunting) is fully complete — see `CHANGELOG.md`.** **Phase 2 (componentization) is actively in progress, with substantial work still open** — detailed below. **Phase 3 (UI uniformity) has not yet started.**

**Phase 2 status**: the 6 cinematic overlay components' consolidation (video-layer hook, fade keyframes, film grain layer, `formatNames()`) is fully complete — see `CHANGELOG.md`. Everything below this point is still open, unimplemented work — a systematic 6-category audit (className strings → JSX structure → utility functions → hook patterns → data structures/constants → server-side patterns) originally surfaced 35 real, evidence-backed findings: 33 are now resolved and closed (see `CHANGELOG.md` for the full history), leaving 2 genuinely open below. Categories 1, 3, 4, and 6 have no open findings left at all — only Category 2 (JSX structural patterns) and Category 5 (data structures/constants) still have one open item each.

**Leads from this session's own file-size census, not yet investigated as deeply as the systematic audit below**: `NpcFormFields.tsx`'s `Abilities`/`Stat Block` tabs are still inline while `Identity`/`Combat` were already extracted — an inconsistent decomposition worth finishing. `useParty.ts` (592 lines after its prior pure-function extraction) and `useCombatSync.ts`/`ActiveEncounterTab/index.tsx` are worth a fresh size check given everything layered on since. `campaigns.ts`'s inlined 7-sheet schema definition overlaps conceptually with `schema.md` and `useSheetSync.ts`'s expectations — three places that need to agree on one schema, currently with no single source of truth. A reusable loading-spinner component was also noticed (inline `Loader2` used with slightly varying sizes across several buttons/dialogs) but not yet scoped. **Server-side error response shaping** — `campaigns.ts` and `auth.ts` both hand-build `{ error, message }` JSON error responses with a similar shape but not identical conventions; not a UI component, but the same "one place should own this format" logic applies, worth a real comparison before deciding if it's worth consolidating. **Possibly a shared tabbed-entity-form pattern** (`NpcFormFields.tsx` and `NewPlayerDialog.tsx` are structurally similar — Identity/Combat/Abilities + one more tab) — genuinely unverified whether this is real duplication or the same kind of surface-level similarity that the `CardHeader` investigation correctly found didn't hold up once compared directly; needs an honest side-by-side check, not an assumption either way, before treating it as a real candidate.

**Systematic componentization audit — Category 2 (repeated JSX structural patterns), findings logged, not yet implemented.**

1. **Two-column form field grid** (`grid grid-cols-2 gap-4`) — reported 5 times across 3 files. Likely too generic/low-abstraction-value to warrant a shared component (same category as the "empty-state displays" pattern from the original Shared UI Component Consolidation audit, which was correctly judged not worth componentizing) — not yet decided either way, flagged for a real decision rather than defaulting to "build it."

**Systematic componentization audit — Category 4 (repeated hook patterns), findings logged, not yet implemented.**

**Systematic componentization audit — Category 5 (repeated data structures/constants), findings logged, not yet implemented.**

2. **Spreadsheet row schemas defined twice** — `campaigns.ts`'s inline sheet-header arrays (used when seeding a new campaign) and `sheetSchemas.ts`'s Zod tuple schemas (used when parsing synced data back) independently encode the identical column order for all 4 core sheets (Characters: 26 columns, NPCs: 22, Encounters: 7, Encounter_Combatants: 14) — confirmed with real quoted field lists matching exactly, not just similar. No single shared source of truth; the two could silently drift if either is changed without updating the other.

### 🔵 Architecture / Technical Debt

**Remaining Technical Debt:**

- **`abilityScores.ts` and `spellcasting.ts` have a circular import.** `abilityScores.ts`'s `parseProficiencies()` calls `parseSpellcastingAbility()` from `spellcasting.ts`; `spellcasting.ts` needs `abilitiesInOrder`/`AbilityName` from `abilityScores.ts`. Confirmed currently safe — every cross-import is used only inside function bodies, never at either file's top level, so ES modules' live-binding semantics handle this correctly with no runtime issue. Not pre-existing to any recent change; surfaced while relocating `abilitiesInOrder` to its correct domain home. A genuine fix would require either moving `parseSpellcastingAbility`/`SpellcastingAbility` into `abilityScores.ts` (a domain mismatch — that logic is conceptually about spellcasting, not ability scores) or restructuring `parseProficiencies()` to no longer call into `spellcasting.ts` directly (would change a working function's signature and require updating every caller). Deliberately deferred — real complexity/risk for a purely architectural benefit, not a functional one.
- **54 pre-existing TypeScript errors across 18 test files, surfaced by a real `npx tsc --noEmit` run (the default, broader config) while verifying an unrelated fix.** Not caused by, or related to, any change made this session — the same run against `npx tsc -p tsconfig.build.json --noEmit` (the actual build-scoped config) was completely clean. Every one of the 54 is a test-file type mismatch: mock/fixture objects missing properties that real interfaces now require (`Encounter` missing `status`/`difficultyName`, `Combatant` missing `ac`/`maxHp`/`currentHp`/`passivePerception`, `CombatState` missing `combatStarted`/`actionContext`, `NPC` missing/gaining fields, a `deleteDimension` property referenced that doesn't exist on `BatchRequest`, `WriteValue` not assignable to `string` in a few sheet-write assertions, 2 unused `@ts-expect-error` directives). Likely accumulated gradually as real interfaces evolved without every test fixture being updated to match — worth a real triage pass at some point, but out of scope for anything done this session.

**Future audit categories, discussed but not yet started** — beyond bug-hunting and componentization (Phase 1/2 above), a professional React/TypeScript codebase review typically also covers several categories this project hasn't yet run as a dedicated pass:

- **Type safety** — a systematic sweep for `any`/unsafe type casts, missing null checks, and places where TypeScript's strictness is being worked around rather than honored. Only ever caught incidentally so far (e.g. the `as any` shortcut proposed and rejected during the `useSettings.ts` type-safety fix, then properly resolved via a narrow, justified cast instead).
- **Accessibility (a11y)** — semantic HTML, ARIA attributes, keyboard navigation, focus management. Also only ever caught incidentally (`IconButton`/`CardHeaderChevron` needing real `aria-label`s, `Tabs.tsx` needing genuine `role="tablist"` + arrow-key navigation, `Accordion.tsx` needing to be a real `<button>` instead of a `div` with manually-reimplemented keyboard handling) — a dedicated pass would likely find more.
- **Security** — this app handles real OAuth tokens and writes to a GM's actual Google Sheets, so this matters more than average for an app this size. Worth checking `dangerouslySetInnerHTML` usage, how tokens are stored/read, and injection risk from user-generated content (NPC names, notes, etc.) rendered without sanitization. One real OAuth CSRF bug was already found and fixed earlier this session — a dedicated security pass might find more in the same vein.
- **Performance** — unnecessary re-renders and missing memoization, checked carefully rather than blanket-applying `useMemo`/`useCallback` everywhere (this session already learned that lesson directly — the `useAppState.ts`/`useMoodPresets.ts` fixes were about correcting genuinely broken memoization, not adding memoization indiscriminately). Also worth checking for expensive computations re-running on every render unnecessarily.
- **Error handling consistency** — server-side error-response shaping was already fully audited and fixed (see `CHANGELOG.md`), but client-side async error handling hasn't been checked for consistency — are failures always caught, and always surfaced to the GM the same way, or does the pattern vary file to file?
- **Code smells beyond duplication** — magic numbers/strings that should be named constants, deeply nested conditionals, dead code or unused exports, "god functions" doing too many unrelated things (a function can be short in line count and still be doing 5 unrelated things at once — a different problem than file size, which Phase 2's census already covers).

Not yet scoped as concrete audit prompts — to be run the same way as the componentization audit (isolated, evidence-required categories) once prioritized.